---
import Layout from '../../layouts/Layout.astro';
import { supabase, type Post } from '../../lib/supabase';

const { slug } = Astro.params;

if (!slug) {
  throw new Error('Slug parameter is required');
}

let post = null;
let error = null;

const { data, error: fetchError } = await supabase
  .from('posts')
  .select('*')
  .eq('slug', slug)
  .single();

post = data;
error = fetchError;

if (error || !post) {
  throw new Error(`Post not found: ${slug}`);
}

export async function getStaticPaths() {
  const { data: posts } = await supabase
    .from('posts')
    .select('slug');
  
  return posts?.map((post) => ({
    params: { slug: post.slug },
  })) || [];
}
---

<Layout>
  <main class="container">
    <nav class="breadcrumb">
      <a href="/" class="back-link">← Back to Posts</a>
    </nav>

    <article class="post">
      <header class="post-header">
        <div class="post-image">
          <img src={post.image_url} alt={post.title} />
        </div>
        <div class="post-header-content">
          <h1 class="post-title">{post.title}</h1>
          <div class="post-meta">
            <time class="post-date">
              {new Date(post.created_at).toLocaleDateString('en-US', {
                year: 'numeric',
                month: 'long',
                day: 'numeric'
              })}
            </time>
            {post.link_two_title && (
              <span class="post-category">{post.link_two_title}</span>
            )}
          </div>
        </div>
      </header>

      <div class="post-content">
        <div class="content-text">
          {post.content.split('\n').map(paragraph => (
            <p>{paragraph}</p>
          ))}
        </div>
      </div>

      {(post.link_one_title || post.link_two_title || post.link_three_title) && (
        <footer class="post-footer">
          <div class="post-links">
            {post.link_one_title && post.link_one_url && (
              <a href="https://www.deviantart.com/ioartseu" class="post-link" target="_blank" rel="noopener noreferrer">
                Buy Vector
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M7 17L17 7M17 7H7M17 7V17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
            )}
            {post.link_two_title && post.link_two_url && (
              <a href={post.link_two_url} class="post-link" target="_blank" rel="noopener noreferrer">
                {post.link_two_title}
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M7 17L17 7M17 7H7M17 7V17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
            )}
            {post.link_three_title && post.link_three_url && (
              <a href={post.link_three_url} class="post-link" target="_blank" rel="noopener noreferrer">
                {post.link_three_title}
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M7 17L17 7M17 7H7M17 7V17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
            )}
          </div>
        </footer>
      )}
    </article>
  </main>
</Layout>

<style>
  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem 1rem;
    min-height: 100vh;
  }

  .breadcrumb {
    margin-bottom: 2rem;
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    color: #666;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .back-link:hover {
    color: #4f46e5;
  }

  .post {
    background: white;
    border: 1px solid #e9ecef;
    border-radius: 12px;
    overflow: hidden;
  }

  .post-header {
    position: relative;
  }

  .post-image {
    aspect-ratio: 16/9;
    overflow: hidden;
    background: #f8f9fa;
  }

  .post-image img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    background: #f8f8f8;
  }

  .post-header-content {
    padding: 2rem;
  }

  .post-title {
    font-size: 2.5rem;
    font-weight: 800;
    margin: 0 0 1rem 0;
    color: #1a1a1a;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .post-meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-size: 0.95rem;
  }

  .post-date {
    color: #666;
    font-weight: 500;
  }

  .post-category {
    background: #f1f5f9;
    color: #475569;
    padding: 0.375rem 1rem;
    border-radius: 20px;
    font-weight: 500;
    font-size: 0.875rem;
  }

  .post-content {
    padding: 0 2rem 2rem;
  }

  .content-text {
    font-size: 1.125rem;
    line-height: 1.7;
    color: #374151;
  }

  .content-text p {
    margin: 0 0 1.5rem 0;
  }

  .content-text p:last-child {
    margin-bottom: 0;
  }

  .post-footer {
    padding: 2rem;
    background: #f8f9fa;
    border-top: 1px solid #e9ecef;
  }

  .post-links {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .post-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s ease;
    border: 1px solid transparent;
  }

  .post-link svg {
    opacity: 0.7;
    transition: opacity 0.2s ease;
  }

  .post-link:hover svg {
    opacity: 1;
  }

  .post-link {
    background: #4f46e5;
    color: white;
  }

  .post-link:hover {
    background: #4338ca;
    transform: translateY(-1px);
  }

  @media (max-width: 768px) {
    .container {
      padding: 1rem;
    }
    
    .post-title {
      font-size: 2rem;
    }
    
    .post-header-content {
      padding: 1.5rem;
    }
    
    .post-content {
      padding: 0 1.5rem 1.5rem;
    }
    
    .post-footer {
      padding: 1.5rem;
    }
    
    .post-meta {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.75rem;
    }
    
    .post-links {
      flex-direction: column;
    }
  }
</style>